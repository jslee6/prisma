generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model usertest {
  name   String? @db.VarChar(100)
  age    Int?
  number String? @db.VarChar(100)
  id     Int     @id @default(autoincrement())
}

model sw {
  swName  String? @db.VarChar(100)
  swPrice String? @db.VarChar(100)
  swId    Int     @id
}

model product {
  id          String           @id
  name        String
  description String?
  category    product_category
  price       Float
  stock       Int
  createdAt   DateTime         @default(now())
  updatedAt   DateTime
}

model user {
  age       Int?
  id        String   @id @default(uuid())
  address   String
  createdAt DateTime @default(now())
  email     String   @unique
  firstName String
  lastName  String
  updatedAt DateTime @updatedAt
}

enum product_category {
  FASHION
  BEAUTY
  SPORTS
  ELECTRONICS
  HOME_INTERIOR
  HOUSEHOLD_SUPPLIES
  KITCHENWARE
}
